// In this file, the data of doctors(list of objects) is fetched from storage using some libraries/APIs.
// This list of objects is mapped with Doctor's card template/component as passing each doctor detail as property to component.
// Grid(or other layout) is used to display the doctor's card.

import React from "react";

export default function DoctorDetails()
{
    
    return(
        <div>
            Doctor details
        </div>
    )
}